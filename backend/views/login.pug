extends layout

block content
    br
    h1= "Log in"
    form(method='POST')
        label(for='name') Usuario:
        input(type='text' name='name')
        br
        br
        label(for="password") Contraseña:  
        input(type='password' name='password')
        br
        br
        button(type='submit') Logear
